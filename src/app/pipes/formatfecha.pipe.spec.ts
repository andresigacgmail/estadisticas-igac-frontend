import { FormatfechaPipe } from './formatfecha.pipe';

describe('FormatfechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatfechaPipe();
    expect(pipe).toBeTruthy();
  });
});
